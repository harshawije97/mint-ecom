/* eslint-disable @typescript-eslint/no-explicit-any */
import React from "react";

async function ColorSlugPage({
  params,
  searchParams,
}: {
  params: { slug: string };
  searchParams: any;
}) {
  return <div>ColorSlugPage</div>;
}

export default ColorSlugPage;
